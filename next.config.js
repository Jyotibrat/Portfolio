/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React strict mode
  // Remove `swcMinify` as it's already the default
}

module.exports = nextConfig;
